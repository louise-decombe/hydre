{% extends 'base.html.twig' %}

{% block title %}Valider ma commande{% endblock %}

{% block content %}

<h2> passer la commande </h2>
<p> Préférences </p>


<p> Coordonnés de facturation </p>

{% set formHtml %}
{{  form_start(form, {action:path('order_recap')} ) }}
{{ form_label(form.addresses, 'choissiez les coordonnés de la commande')}}
<a href="{{ path('profil_address_add')}}"> ajouter une adresse </a>
{{  form_end(form) }}


 {% endset %}

 {{  formHtml|replace({'[br]': '<br>'})|raw }}

<p> Récapitulatif de la commande </p>
  {% for product in cart %}

<img src="/uploads/{{product.product.illustration}}" alt="{{product.product.name}}" height="100px">

{{product.product.name}}

{{(product.product.price / 100) | number_format(2) }}euros
 | x {{product.quantity}}


{% endfor %}


{% endblock %}
